function writeBovHeader() result(retVal)
    use globals
    use mainModule
    use particleModule
    use frameModule
    implicit none
    character(50) :: frameStr, uStr, vStr, wStr
    logical :: retVal
end function writeBovHeader
